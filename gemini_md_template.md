# 題目編號與名稱：LeetCode [題號] - [題目名稱]

## 題目連結

[這裡放 LeetCode 題目的 URL 連結]

## 題目分析與思考

* **關鍵字 / 核心概念**：
    * [列出題目中的關鍵資訊，例如：`排序陣列`, `子序列`, `二維矩陣` 等]
    * [思考這題可能屬於哪個演算法或資料結構，例如：`動態規劃`, `雙指針`, `哈希表` 等]

* **我的初步思路**：
    1.  [記錄你的第一種想法，例如：`暴力法遍歷所有可能`]
    2.  [分析這種方法的優缺點，例如：`時間複雜度為 O(n^2)`, `空間複雜度為 O(1)`]

* **第二種思路 / 優化想法**：
    1.  [記錄你的第二種想法，例如：`使用哈希表來優化查找過程`]
    2.  [分析這種方法的優缺點，例如：`時間複雜度優化為 O(n)`, `但空間複雜度增加為 O(n)`]

## 最終解法與程式碼

**演算法/資料結構**：[最終使用的演算法或資料結構，例如：`哈希表 (Hash Table)`]

**程式碼 (Python)**：
```python
# 將你的 Python 程式碼貼在這裡

def two_sum(nums, target):
    # 程式碼註解
    pass
```  
