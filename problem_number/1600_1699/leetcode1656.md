# 1656. Design an Ordered Stream

https://leetcode.com/problems/design-an-ordered-stream

## 提示  
`String[] insert(int idKey, String value)` 的 `idKey` 等於最左邊還未被輸出的 `idKey` 時處理輸出，效率夠好。

## 解題過程  
沒想清楚，比對了整段 [最左邊還未被輸出的 `idKey`:這次輸入的 `idKey`]。